# Copy to .env for local runs. .env is git-ignored and never leaves this machine.
BUFFER_API_KEY=
CF_ACCOUNT_ID=
CF_API_TOKEN=
# Only needed locally; in GitHub Actions this is derived automatically.
PAGES_BASE_URL=
